services:
  application:
    image: golang:1.26
    working_dir: /app
    volumes:
      - .:/app
    command: go run ./cmd/main.go

  postgres:
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
